# ct_commodity_db_type

This function is deprecated. There is currently no alternative for this
function. **\[superseded\]**

## Usage

``` r
ct_commodity_db_type(...)
```

## Arguments

- ...:

  Used to catch all possible arguments that users have supplied to this
  function.

## Value

depreciation error

## Examples

``` r
if (FALSE) { # interactive()
# no examples because only legacy function
}
```
