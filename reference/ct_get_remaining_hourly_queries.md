# ct_get_remaining_hourly_queries

This function is deprecated. There is no more reset time, as the upper
limit of 250 calls per day is enforced daily. **\[superseded\]**

## Usage

``` r
ct_get_remaining_hourly_queries(...)
```

## Arguments

- ...:

  Used to catch all possible arguments that users have supplied to this
  function.

## Value

depreciation error

## Examples

``` r
if (FALSE) { # interactive()
# no examples because only legacy function
}
```
