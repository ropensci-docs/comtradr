# ct_update_databases

This function is deprecated. Please use `update` parameter in the main
`ct_get_data` function instead. **\[superseded\]**

## Usage

``` r
ct_update_databases(...)
```

## Arguments

- ...:

  Used to catch all possible arguments that users have supplied to this
  function.

## Value

depreciation error

## Examples

``` r
if (FALSE) { # interactive()
# no examples because only legacy function
}
```
