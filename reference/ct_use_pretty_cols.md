# ct_use_pretty_cols

This function is deprecated. Please use the `process` argument in the
main function instead. **\[superseded\]**

## Usage

``` r
ct_use_pretty_cols(...)
```

## Arguments

- ...:

  Used to catch all possible arguments that users have supplied to this
  function.

## Value

depreciation error

## Examples

``` r
if (FALSE) { # interactive()
# no examples because only legacy function
}
```
